if (!(a == 2)) { };  // Noncompliant {{请使用相反的运算符 （"!="）。}}
//  ^^^^^^^^^
!(i < 10);  // Noncompliant {{请使用相反的运算符 （">="）。}}
!(i > 10);  // Noncompliant {{请使用相反的运算符 （"<="）。}}
!(i != 10);  // Noncompliant {{请使用相反的运算符 （"=="）。}}
!(i <= 10);  // Noncompliant {{请使用相反的运算符 （">"）。}}
!(i >= 10);  // Noncompliant {{请使用相反的运算符 （"<"）。}}

if (a != 2) { };
(i >= 10);
!(a + i);