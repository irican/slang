// Noncompliant@+2 {{删除此处被注释掉的代码。}}
foo();
// if(a) { print(b); };
foo();
// this is a normal comment
bar();
// Noncompliant@+2
a = 1;
/*
 fun foo() {
   print(a);
   if (a) return "hello world!"
 }
*/
